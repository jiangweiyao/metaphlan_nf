process.executor = "sge"
process.queue = "all.q"
process.penv = 'smp'
singularity.runOptions = "--bind /scicomp/reference --bind /scicomp/groups --bind /scicomp/home --bind /scicomp/reference"

executor.queueSize = 100

//NXF_OPTS="-Xms1024m -Xmx102400m"
